# {{ repo.name }}{{#if travis}} {{> travis.md }} {{/if}}

> {{{ repo.description }}}

{{> body }} {{! include content from `src/content/*.md` }}

## Release History
{{#if changelog}}{{#each changelog}} * {{ date }}    {{ version }}    {{#each changes}}{{{.}}} {{/each}}
{{/each}}
{{else}}_no changes_{{/if}}

---
Authored by [{{ repo.author.name }}]({{ repo.author.url }})

_This file was generated on {{ today }}._
