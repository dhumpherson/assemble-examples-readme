{{! this partial can be used as an alternative to the built-in helper }}
{{#if docs.changelog}}{{#each changelog}}* {{{ date }}}    {{{ @key }}}    {{#each changes}}{{{.}}} {{/each}}
{{/each}}{{else}}_(Nothing yet)_{{/if}}